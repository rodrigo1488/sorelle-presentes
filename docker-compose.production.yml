# Alias da stack de produção — use o docker-compose.yml principal:
#
#   cp .env.docker.example .env.docker
#   docker compose --env-file .env.docker up -d --build

include:
  - path: docker-compose.yml
